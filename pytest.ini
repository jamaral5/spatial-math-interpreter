[pytest]
testpaths = tests
# The handler uses flat imports (`from config import CONFIG`) because that is how
# Lambda lays out sys.path. conftest.py reproduces that; this keeps pytest from
# also inserting rootdir and creating a second, different import path.
addopts = -q
